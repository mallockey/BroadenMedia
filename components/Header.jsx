import React from 'react'

const Header = (props) => {

  return(
    <div id="header">
      <img src="/images/logo.png" className="headerLogo"></img>
      <a href="/">About</a>
    </div>
  )
}

export default Header
